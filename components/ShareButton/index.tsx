import React from 'react';
import { View } from 'react-native';
import { Icon } from 'react-native-elements';

const ShareButton = () => (
  <View style={{ marginRight: 20 }}>
    <Icon type="font-awesome" name="upload" color="#fff" size={17}></Icon>
  </View>
);

export default ShareButton;
